# API base URL without trailing slash (optional).
# If unset, the app uses https://toskanini-backend.onrender.com
# Set to empty to use the Vite dev proxy: VITE_API_URL=
# Local backend: VITE_API_URL=http://127.0.0.1:5001
